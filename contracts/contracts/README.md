These contracts enable a global registry to register user registries that own multiple contracts for minting tokens.

The Global Registry contract follows the proxy pattern as defined in ERC-1167
ref: https://docs.openzeppelin.com/contracts/5.x/api/proxy#clones